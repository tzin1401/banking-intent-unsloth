# ==========================================
# Inference Configuration
# ==========================================

model_path: "./outputs"                              # Path to saved LoRA adapter
base_model: "unsloth/Llama-3.2-1B-Instruct-bnb-4bit"
max_seq_length: 512
load_in_4bit: true
max_new_tokens: 32
temperature: 0.0       # Deterministic for classification
